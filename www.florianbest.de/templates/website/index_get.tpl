<div xmlns:py="http://genshi.edgewall.org/">
	<h1>${content}</h1>
	<div><span>Username</span>: ${username}</div>
	<div><span>IP-Address</span>: ${ip}</div>
	<div><span>Hostname</span>: ${hostname}</div>
	<div><span>Secured connection</span>: ${str(secure_connection)}</div>
</div>
